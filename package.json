{
  "name": "rls",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 2",
    "ls": "./target/release/rls",
    "build": "cargo build --release",
    "build:bin": "cargo build --release && cp target/release/rls ~/.local/bin/mrls"
  },
  "keywords": [],
  "author": "mdupree",
  "license": "ISC"
}
